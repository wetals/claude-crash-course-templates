## Phase 2 - Stubs continuation

You are now continuing with Phase 2 of the app development process. Your task is to review the files in the `Project knowledge` and continue to create a stubbed-out project structure based on the `masterplan.md` file generated in Phase 1 and any drawings or wireframes provided by the user.

## Follow these instructions:

Begin by reviewing the `masterplan.md` file and the files in the `Project knowledge` to understand which files has been stubbed-out so far and which files are left to be stubbed-out. Familiarize yourself with the app's overall structure, main features, and architectural decisions.
Review and understand the high-level project structure of the files created in the `Project knowledge`. This reflects the app's architecture (e.g., frontend, backend, database) and main features.

For each major component or feature identified in the `masterplan.md`, create stub files with minimal, essential information. Include:

- A brief comment at the top of each file describing its purpose
- Placeholder import statements for likely dependencies
- Empty function or class declarations for key functionalities
- TODO comments indicating where major logic will be implemented

In the stub files, do not include detailed implementations or actual code logic.
The goal is to create a skeleton that can be easily expanded in Phase 3.

## For the frontend (if applicable):

- Create basic component files with empty component declarations
- Stub out main pages or views
- Include placeholder routing information

## For the backend (if applicable):

- Create stub files for main API endpoints or services
- Include empty model definitions based on the data structure outlined in the `masterplan.md`
- Stub out database connection file (if needed)

## Create a basic README.md file with:

- Project title and brief description
- Placeholder sections for setup instructions, main features, and technology stack

Ask the user if they would like to see the content of any specific stubbed files, and be prepared to show and explain them.
Remind the user that this is a minimal structure and that full implementation will occur in Phase 3.

## Remember:

- Keep all stubs minimal. Include just enough information to understand the file's purpose and it's place in the overall structure.
- Do not implement any actual logic or detailed code.
- Use consistent naming conventions throughout the project structure.
- If you're unsure about a specific structure or file, refer back to the `masterplan.md` or ask the user for clarification.

Begin by acknowledging that you're ready to continue with Phase 2 and ask the user if they're ready to see the stubbed-out content of any specific files based on the `masterplan.md`
